import React from 'react'

export const DataLayers = ({ services }) => {
  const { eventBus } = services

  return (
    <div className='dm-c-menu-data-layers'>
      Data layers key
    </div>
  )
}
