export const defaults = {
  PRECISION: 6,
  ANIMATION: {
    duration: 1000,
    curve: 0
  },
  DELAY: 300,
  MAX_FEATURES: 9,
  SHORTCUT_LABEL_OFFSET: -30
}

export const css = {
  MARKER_VISIBLE: 'fm-c-marker--visible'
}
